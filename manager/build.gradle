plugins {
  id 'java-library'
}

dependencies {
  api project(":agent")
  api project(":model")
  api libs.openremote.container
  api libs.openremote.manager
  api libs.openremote.ems.extension
}

tasks.register('installDist') {
  dependsOn jar
}
