{
  "name": "cityselector-app",
  "version": "1.0.3",
  "description": "City Selector app",
  "author": "OpenRemote",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "exports": {
    "./*": "./lib/*.js"
  },
  "scripts": {
    "clean": "tsc -b --clean && shx rm -rf dist",
    "modelBuild": "orutil build",
    "modelWatch": "orutil watch",
    "build": "cross-env NODE_OPTIONS=--max_old_space_size=4096 rspack --mode production",
    "serve": "tsc -b --clean && shx rm -rf dist && orutil build && cross-env NODE_OPTIONS=--max_old_space_size=4096 rspack serve --mode development",
    "serveProd": "tsc -b --clean && shx rm -rf dist && orutil build && cross-env NODE_OPTIONS=--max_old_space_size=4096 rspack serve --mode production",
    "serveNoModelBuild": "tsc -b --clean && cross-env NODE_OPTIONS=--max_old_space_size=4096 rspack serve --mode development",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@lit/task": "^1.0.0",
    "@openremote/or-mwc-components": "~1.26.1",
    "lit": "^3.3.2",
    "model": "workspace:*",
    "rest": "workspace:*"
  },
  "devDependencies": {
    "@openremote/util": "~1.26.1",
    "@rspack/cli": "~1.7.10",
    "@rspack/core": "~1.7.10",
    "cross-env": "^10.1.0",
    "shx": "~0.4.0",
    "typescript": "5.8.3",
    "webpack": "^5.108.3",
    "webpack-bundle-analyzer": "^4.10.2"
  }
}
